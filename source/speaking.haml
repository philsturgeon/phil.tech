---
layout: page
title: Speaking
---

%p All my friends know I never shut up, and they're all happy when I bugger off to far flung corners of the world to talk somebody elses ear off for a bit.

- { upcoming: "Upcoming", past: "Past" }.each do |collection, header|

  %h2= header

  - data.speaking[collection].each do |speak|
    .talk
      %header
        %h1
          %a{href: speak.event.url}=h speak.event.name
        %p=h "#{speak.event.dates} in #{speak.city}, #{speak.country}"

      %article.talk-content
        - speak.talks.each do |talk|
          %h3=h data.talks.recycled[talk].title
          %p=h data.talks.recycled[talk].short

          %div.button-group
            - if data.talks.recycled[talk].slides
              %a{href: data.talks.recycled[talk].slides}
                %button.buy-book Slides

            - if data.talks.recycled[talk].video
              %a{href: data.talks.recycled[talk].video}
                %button.buy-book Video
